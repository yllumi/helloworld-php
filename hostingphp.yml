project_name: Helloworld
framework: php
database:
  name: helloworld
  user: helloworld
  password: helloworld
  host: localhost
  port: 3306
  auto_migrate: true
  create_user: true
  tables:
    - name: mahasiswa
      fields:
        - id: id
          type: INT(5)
          attributes: "AUTO_INCREMENT PRIMARY KEY"
        - name: nama
          type: VARCHAR(100)
        - name: nim
          type: VARCHAR(10)